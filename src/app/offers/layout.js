export const metadata = {
  title: 'Active Sellers | Pasa.ph',
  description: 'Find sellers heading to your location who can buy items for you.',
};

export default function OffersLayout({ children }) {
  return children;
}