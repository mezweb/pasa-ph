export const metadata = {
  title: 'Sign Up | Pasa.ph',
  description: 'Create your Pasa.ph account to start buying or selling globally.',
};

export default function SignupLayout({ children }) {
  return children;
}