export const metadata = {
  title: 'Seller Marketplace | Pasa.ph',
  description: 'Browse high-demand items you can fulfill as a traveler.',
};

export default function ProductsLayout({ children }) {
  return children;
}