export const metadata = {
  title: 'Contact Support | Pasa.ph',
  description: 'Get help with your orders or account on Pasa.ph.',
};

export default function SupportLayout({ children }) {
  return children;
}