export const metadata = {
  title: 'Login | Pasa.ph',
  description: 'Log in to start posting requests or earning as a seller.',
};

export default function LoginLayout({ children }) {
  return children;
}