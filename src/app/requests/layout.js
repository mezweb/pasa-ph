export const metadata = {
  title: 'Marketplace Requests | Pasa.ph',
  description: 'Browse active pasabuy requests from Bulacan, Pampanga, and Metro Manila.',
};

export default function RequestsLayout({ children }) {
  return children;
}